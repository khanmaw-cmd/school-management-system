import {createClient} from "@/lib/supabase/server";import {addYear} from "./actions";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:m}=await s.from("school_memberships").select("school_id").eq("user_id",user!.id).eq("active",true).limit(1).single();const {data:years}=await s.from("academic_years").select("*").eq("school_id",m!.school_id).order("starts_on",{ascending:false});return <main className="p-6 md:p-10"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Academic Years</h1><form action={addYear} className="mt-6 grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-4"><input name="name" required placeholder="2026-27" className="rounded-xl border p-3"/><input name="starts_on" required type="date" className="rounded-xl border p-3"/><input name="ends_on" required type="date" className="rounded-xl border p-3"/><label><input name="active" type="checkbox"/> Active year</label><button className="rounded-xl bg-slate-900 p-3 text-white">Add academic year</button></form><div className="mt-6 space-y-3">{years?.map(y=><div key={y.id} className="rounded-xl border bg-white p-4"><b>{y.name}</b><span className="ml-3 text-sm text-slate-500">{y.starts_on} to {y.ends_on}</span>{y.active&&<span className="ml-3 text-xs">ACTIVE</span>}</div>)}</div></div></main>}