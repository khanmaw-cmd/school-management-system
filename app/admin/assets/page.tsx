import {requireSchoolUser} from "@/lib/auth";import {addAsset,addCategory,updateCondition} from "./actions";
export default async function Page(){const {supabase:s,school}=await requireSchoolUser(["school_owner","principal","accountant","reception"]);const id=school!.id;const [{data:cats},{data:assets}]=await Promise.all([s.from("asset_categories").select("*").eq("school_id",id).order("name"),s.from("assets").select("*,asset_categories(name)").eq("school_id",id).eq("active",true).order("created_at",{ascending:false})]);return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Assets & Inventory</h1><p className="text-slate-500">Track school equipment, furniture, electronics and other assets.</p><div className="mt-6 grid gap-5 lg:grid-cols-3"><form action={addCategory} className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Asset category</h2><input name="name" required placeholder="e.g. Computers" className="mt-3 w-full rounded-xl border p-3"/><button className="mt-3 w-full rounded-xl bg-slate-900 p-3 text-white">Add category</button></form><form action={addAsset} className="rounded-2xl border bg-white p-5 lg:col-span-2"><h2 className="font-semibold">Register asset</h2><div className="mt-3 grid gap-2 sm:grid-cols-3"><input name="code" required placeholder="Asset code" className="rounded-xl border p-3"/><input name="name" required placeholder="Asset name" className="rounded-xl border p-3"/><select name="category" className="rounded-xl border p-3"><option value="">Category</option>{cats?.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><input name="quantity" type="number" min="0" defaultValue="1" className="rounded-xl border p-3"/><input name="location" placeholder="Location / room" className="rounded-xl border p-3"/><input name="date" type="date" className="rounded-xl border p-3"/><input name="cost" type="number" min="0" step=".01" placeholder="Purchase cost" className="rounded-xl border p-3"/><select name="condition" className="rounded-xl border p-3">{["new","good","fair","repair","retired"].map(x=><option key={x}>{x}</option>)}</select><input name="notes" placeholder="Notes" className="rounded-xl border p-3"/><button className="rounded-xl bg-blue-700 p-3 text-white sm:col-span-3">Register asset</button></div></form></div><section className="mt-6 rounded-2xl border bg-white"><div className="grid grid-cols-5 gap-2 p-4 text-xs font-semibold uppercase text-slate-500"><span>Asset</span><span>Category</span><span>Qty</span><span>Location</span><span>Condition</span></div>{assets?.map((x:any)=><div key={x.id} className="grid grid-cols-5 gap-2 border-t p-4 text-sm"><div><b>{x.name}</b><p className="text-xs text-slate-500">{x.asset_code}</p></div><span>{x.asset_categories?.name||"-"}</span><span>{x.quantity}</span><span>{x.location||"-"}</span><form action={updateCondition} className="flex gap-2"><input type="hidden" name="id" value={x.id}/><select name="condition" defaultValue={x.condition} className="min-w-0 rounded-lg border p-2">{["new","good","fair","repair","retired"].map(v=><option key={v}>{v}</option>)}</select><button className="rounded-lg border px-2">Save</button></form></div>)}</section></div></main>}