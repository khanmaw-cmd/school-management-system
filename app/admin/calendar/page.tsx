import {requireSchoolUser} from "@/lib/auth";import {addEvent} from "./actions";
export default async function Page(){const {supabase:s,school,membership}=await requireSchoolUser(["school_owner","principal","teacher","accountant","reception"]);const id=school!.id,can=["school_owner","principal","reception"].includes(membership.role);const {data:events}=await s.from("school_calendar_events").select("*").eq("school_id",id).order("starts_on").limit(200);return <main className="p-6 md:p-10"><div className="mx-auto max-w-6xl"><h1 className="text-3xl font-bold">School Calendar</h1><p className="text-slate-500">Holidays, events, meetings, activities and exam dates.</p>{can&&<form action={addEvent} className="mt-6 grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-3"><input name="title" required placeholder="Title" className="rounded-xl border p-3"/><select name="type" className="rounded-xl border p-3">{["holiday","event","exam","meeting","activity"].map(x=><option key={x}>{x}</option>)}</select><select name="audience" className="rounded-xl border p-3">{["school","staff","students","parents"].map(x=><option key={x}>{x}</option>)}</select><input name="start" required type="date" className="rounded-xl border p-3"/><input name="end" required type="date" className="rounded-xl border p-3"/><input name="description" placeholder="Description" className="rounded-xl border p-3"/><button className="rounded-xl bg-slate-900 p-3 text-white md:col-span-3">Add to calendar</button></form>}<div className="mt-6 grid gap-3">{events?.map(x=><article key={x.id} className="rounded-2xl border bg-white p-5 md:flex md:items-center md:justify-between"><div><p className="text-xs font-semibold uppercase text-blue-700">{x.event_type} · {x.audience}</p><h2 className="mt-1 font-semibold">{x.title}</h2><p className="text-sm text-slate-500">{x.description}</p></div><b className="mt-3 block text-sm md:mt-0">{x.starts_on}{x.ends_on!==x.starts_on?" → "+x.ends_on:""}</b></article>)}</div></div></main>}