import {requireSchoolUser} from "@/lib/auth";import {addEnquiry,updateEnquiry} from "./actions";
export default async function Page(){const {supabase:s,school}=await requireSchoolUser(["school_owner","principal","reception"]);const {data:rows}=await s.from("admission_enquiries").select("*").eq("school_id",school!.id).order("created_at",{ascending:false}).limit(200);return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Admission Enquiries</h1><p className="text-slate-500">Capture leads, schedule follow-ups and track admission conversion.</p><form action={addEnquiry} className="mt-6 grid gap-2 rounded-2xl border bg-white p-5 md:grid-cols-4">{[["student","Student name"],["guardian","Guardian"],["phone","Phone"],["email","Email"],["class","Class interested"],["source","Source"]].map(x=><input key={x[0]} name={x[0]} required={x[0]==="student"||x[0]==="phone"} placeholder={x[1]} className="rounded-xl border p-3"/>)}<input name="follow" type="date" className="rounded-xl border p-3"/><input name="notes" placeholder="Notes" className="rounded-xl border p-3"/><button className="rounded-xl bg-blue-700 p-3 text-white md:col-span-4">Add enquiry</button></form><section className="mt-6 rounded-2xl border bg-white">{rows?.map(x=><div key={x.id} className="grid gap-3 border-b p-4 text-sm lg:grid-cols-6 lg:items-center"><div><b>{x.student_name}</b><p className="text-slate-500">{x.guardian_name}</p></div><span>{x.phone}</span><span>{x.class_interest||"-"}</span><span>{x.next_follow_up||"No follow-up"}</span><form action={updateEnquiry} className="contents"><input type="hidden" name="id" value={x.id}/><select name="status" defaultValue={x.status} className="rounded-lg border p-2">{["new","contacted","follow_up","converted","closed"].map(v=><option key={v}>{v}</option>)}</select><div className="flex gap-1"><input name="follow" type="date" defaultValue={x.next_follow_up||""} className="min-w-0 rounded-lg border p-2"/><button className="rounded-lg border px-3">Save</button></div></form></div>)}</section></div></main>}