import {requireSchoolUser} from "@/lib/auth";import {addGrade} from "../actions";
export default async function Page(){const {supabase:s,school}=await requireSchoolUser(["school_owner","principal"]);const {data:g}=await s.from("grade_scales").select("*").eq("school_id",school!.id).order("min_percentage",{ascending:false});return <main className="p-6 md:p-10"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Grade Scale</h1><p className="mt-1 text-slate-500">Define percentage ranges used on report cards.</p><form action={addGrade} className="mt-6 grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-3"><input name="name" defaultValue="Default" required className="rounded-xl border p-3"/><input name="grade" required placeholder="A1 / A / B" className="rounded-xl border p-3"/><input name="remark" placeholder="Excellent" className="rounded-xl border p-3"/><input name="min" required type="number" min="0" max="100" step=".01" placeholder="Min %" className="rounded-xl border p-3"/><input name="max" required type="number" min="0" max="100" step=".01" placeholder="Max %" className="rounded-xl border p-3"/><input name="sort" type="number" defaultValue="0" className="rounded-xl border p-3"/><button className="rounded-xl bg-slate-900 p-3 text-white">Add grade</button></form><div className="mt-6 rounded-2xl border bg-white">{g?.map(x=><div key={x.id} className="grid grid-cols-4 border-b p-4 text-sm"><b>{x.grade}</b><span>{x.min_percentage}%–{x.max_percentage}%</span><span>{x.remark||"-"}</span><span>{x.name}</span></div>)}</div></div></main>}