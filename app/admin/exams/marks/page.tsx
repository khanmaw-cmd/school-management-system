import {requireSchoolUser} from "@/lib/auth";import {saveMarks} from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams,{supabase:s,school}=await requireSchoolUser(["school_owner","principal","teacher"]);const id=school!.id,{data:es}=await s.from("exam_subjects").select("id,class_id,max_marks,pass_marks,exams(id,name,academic_year_id),subjects(name),classes(name)").eq("school_id",id);let students:any[]=[],existing:any[]=[];const selected=es?.find(x=>x.id===q.exam_subject);if(selected){const exam=Array.isArray(selected.exams)?selected.exams[0]:selected.exams;const [{data:e},{data:m}]=await Promise.all([s.from("student_enrollments").select("students(id,admission_no,first_name,last_name)").eq("school_id",id).eq("academic_year_id",exam.academic_year_id).eq("class_id",selected.class_id),s.from("exam_marks").select("student_id,marks,absent,remarks").eq("school_id",id).eq("exam_subject_id",selected.id)]);students=e||[];existing=m||[]}const old=new Map(existing.map(x=>[x.student_id,x]));return <main className="p-6 md:p-10"><div className="mx-auto max-w-6xl"><h1 className="text-3xl font-bold">Marks Entry</h1><form className="mt-6 flex gap-3"><select name="exam_subject" defaultValue={q.exam_subject} required className="flex-1 rounded-xl border bg-white p-3"><option value="">Select exam / class / subject</option>{es?.map((x:any)=><option key={x.id} value={x.id}>{x.exams?.name} · {x.classes?.name} · {x.subjects?.name} ({x.max_marks})</option>)}</select><button className="rounded-xl bg-blue-700 px-5 text-white">Load</button></form>{selected&&<form action={saveMarks} className="mt-6 rounded-2xl border bg-white p-5"><input type="hidden" name="exam_subject" value={selected.id}/><p className="mb-4 text-sm text-slate-500">Maximum {selected.max_marks} · Pass {selected.pass_marks}</p>{students.map((e:any)=>{const st=Array.isArray(e.students)?e.students[0]:e.students,p=old.get(st.id);return <div key={st.id} className="grid grid-cols-5 items-center gap-2 border-b py-3 text-sm"><input type="hidden" name="student_id" value={st.id}/><span>{st.admission_no}</span><b>{st.first_name} {st.last_name}</b><input name={"marks_"+st.id} type="number" min="0" max={Number(selected.max_marks)} step=".01" defaultValue={p?.marks??""} placeholder="Marks" className="rounded-lg border p-2"/><label><input name={"absent_"+st.id} type="checkbox" defaultChecked={p?.absent||false}/> Absent</label><input name={"remarks_"+st.id} defaultValue={p?.remarks||""} placeholder="Remarks" className="rounded-lg border p-2"/></div>})}{students.length?<button className="mt-5 rounded-xl bg-slate-900 px-6 py-3 text-white">{existing.length?"Update marks":"Save marks"}</button>:<p className="p-6 text-center text-slate-500">No students enrolled for this exam academic year/class.</p>}</form>}</div></main>}