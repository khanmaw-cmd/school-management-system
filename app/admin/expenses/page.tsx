import {requireSchoolUser} from "@/lib/auth";import {addExpense} from "./actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams,{supabase:s,school}=await requireSchoolUser(["school_owner","principal","accountant"]);const month=q.month||new Date().toISOString().slice(0,7),start=month+"-01";const [y,m]=month.split("-").map(Number),end=new Date(Date.UTC(y,m,1)).toISOString().slice(0,10);const {data:rows}=await s.from("school_expenses").select("*").eq("school_id",school!.id).gte("expense_date",start).lt("expense_date",end).order("expense_date",{ascending:false});const total=(rows||[]).reduce((n,x)=>n+Number(x.amount),0);return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Expenses</h1><div className="mt-4 rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">Expenses this month</p><b className="text-3xl">₹{total.toLocaleString("en-IN")}</b></div><form action={addExpense} className="mt-5 grid gap-2 rounded-2xl border bg-white p-5 md:grid-cols-4"><input name="date" required type="date" className="rounded-xl border p-3"/><input name="category" required placeholder="Category" className="rounded-xl border p-3"/><input name="description" required placeholder="Description" className="rounded-xl border p-3"/><input name="amount" required type="number" min=".01" step=".01" placeholder="Amount" className="rounded-xl border p-3"/><select name="method" className="rounded-xl border p-3">{["cash","upi","bank","card","other"].map(x=><option key={x}>{x}</option>)}</select><input name="vendor" placeholder="Vendor" className="rounded-xl border p-3"/><input name="reference" placeholder="Reference" className="rounded-xl border p-3"/><button className="rounded-xl bg-slate-900 p-3 text-white">Record expense</button></form><form className="mt-5"><input name="month" type="month" defaultValue={month} className="rounded-xl border p-3"/><button className="ml-2 rounded-xl border bg-white px-4 py-3">View</button></form><section className="mt-3 rounded-2xl border bg-white">{rows?.map(x=><div key={x.id} className="grid gap-2 border-b p-4 text-sm md:grid-cols-5"><span>{x.expense_date}</span><b>{x.category}</b><span>{x.description}</span><span>{x.vendor||x.payment_method}</span><b>₹{Number(x.amount).toLocaleString("en-IN")}</b></div>)}</section></div></main>}