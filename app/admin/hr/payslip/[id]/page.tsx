import {requireSchoolUser} from "@/lib/auth";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase:s,school}=await requireSchoolUser(["school_owner","principal","accountant"]);const {data:item}=await s.from("payroll_items").select("*,staff(full_name,employee_no,designation),payroll_runs(pay_month,status)").eq("id",id).eq("school_id",school!.id).maybeSingle();if(!item)return <main className="p-8">Payslip not found.</main>;const run=Array.isArray(item.payroll_runs)?item.payroll_runs[0]:item.payroll_runs;const staff=Array.isArray(item.staff)?item.staff[0]:item.staff;return <main className="mx-auto max-w-2xl p-6 print:p-0"><div className="rounded-2xl border bg-white p-8 print:border-0"><div className="flex justify-between border-b pb-5"><div><h1 className="text-2xl font-bold">{school!.name}</h1><p className="text-slate-500">Salary Payslip</p></div><button onClick={undefined} className="print:hidden"></button></div><div className="grid grid-cols-2 gap-4 py-6 text-sm"><div><b>Employee</b><p>{staff?.full_name}</p></div><div><b>Employee No.</b><p>{staff?.employee_no}</p></div><div><b>Designation</b><p>{staff?.designation||"—"}</p></div><div><b>Pay month</b><p>{run?.pay_month}</p></div></div><div className="space-y-3 border-y py-5"><p className="flex justify-between"><span>Basic</span><b>₹{Number(item.basic).toLocaleString("en-IN")}</b></p><p className="flex justify-between"><span>Allowances</span><b>₹{Number(item.allowances).toLocaleString("en-IN")}</b></p><p className="flex justify-between"><span>Deductions</span><b>- ₹{Number(item.deductions).toLocaleString("en-IN")}</b></p><p className="flex justify-between text-lg"><span>Net pay</span><b>₹{Number(item.net_pay).toLocaleString("en-IN")}</b></p></div><p className="mt-5 text-sm">Status: <b className="capitalize">{item.status}</b>{item.paid_on?" · Paid "+item.paid_on:""}</p><p className="mt-8 text-xs text-slate-500">Computer-generated payslip.</p></div></main>}