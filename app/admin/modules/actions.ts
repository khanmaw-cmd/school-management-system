"use server";import {revalidatePath} from "next/cache";import {requireSchoolUser} from "@/lib/auth";export async function toggleModule(f:FormData){const {supabase:s,school}=await requireSchoolUser(["school_owner","principal"]);const {error}=await s.from("school_modules").upsert({school_id:school!.id,module_key:String(f.get("key")),enabled:String(f.get("enabled"))==="true",updated_at:new Date().toISOString()},{onConflict:"school_id,module_key"});if(error)throw new Error(error.message);revalidatePath("/admin/modules")}