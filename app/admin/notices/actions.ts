"use server";import {revalidatePath} from "next/cache";import {createClient} from "@/lib/supabase/server";
export async function addNotice(f:FormData){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)throw new Error("Unauthorized");const {data:m}=await s.from("school_memberships").select("school_id").eq("user_id",user.id).eq("active",true).limit(1).single();await s.from("notices").insert({school_id:m!.school_id,title:String(f.get("title")),body:String(f.get("body")),audience:String(f.get("audience")||"all"),created_by:user.id});revalidatePath("/admin/notices");}