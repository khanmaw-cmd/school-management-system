import {createClient} from "@/lib/supabase/server";import {addNotice} from "./actions";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:m}=await s.from("school_memberships").select("school_id").eq("user_id",user!.id).eq("active",true).limit(1).single();const {data:n}=await s.from("notices").select("*").eq("school_id",m!.school_id).order("created_at",{ascending:false});return <main className="p-6 md:p-10"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Notices & Announcements</h1><form action={addNotice} className="mt-6 rounded-2xl border bg-white p-5"><input name="title" required placeholder="Notice title" className="w-full rounded-xl border p-3"/><select name="audience" className="mt-3 w-full rounded-xl border p-3"><option value="all">Everyone</option><option value="parents">Parents</option><option value="students">Students</option><option value="staff">Staff</option></select><textarea name="body" required placeholder="Announcement" className="mt-3 min-h-28 w-full rounded-xl border p-3"/><button className="mt-3 rounded-xl bg-slate-900 px-5 py-3 text-white">Publish notice</button></form><div className="mt-6 space-y-3">{n?.map(x=><article key={x.id} className="rounded-xl border bg-white p-5"><h2 className="font-semibold">{x.title}</h2><p className="mt-2 text-sm">{x.body}</p><p className="mt-3 text-xs uppercase text-slate-500">Audience: {x.audience}</p></article>)}</div></div></main>}