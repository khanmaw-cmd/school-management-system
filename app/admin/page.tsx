import Link from "next/link";import {requireSchoolUser} from "@/lib/auth";
export default async function AdminPage(){const {supabase:s,school,membership}=await requireSchoolUser();const id=school!.id;const [{count:students},{count:staff},{count:classes},{count:pending}]=await Promise.all([s.from("students").select("*",{count:"exact",head:true}).eq("school_id",id).eq("active",true),s.from("staff").select("*",{count:"exact",head:true}).eq("school_id",id).eq("active",true),s.from("classes").select("*",{count:"exact",head:true}).eq("school_id",id),s.from("student_attendance").select("*",{count:"exact",head:true}).eq("school_id",id).eq("attendance_date",new Date().toISOString().slice(0,10)).eq("status","absent")]);const cards=[["Students","/admin/students"],["Classes & Sections","/admin/classes"],["Teachers","/admin/teachers"],["Attendance","/admin/attendance"],["Fees","/admin/fees"],["Exams","/admin/exams"],["Subjects","/admin/subjects"],["Homework","/admin/homework"],["Notices","/admin/notices"]];return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><p className="text-sm font-semibold text-blue-700">{school?.code}</p><h1 className="text-3xl font-bold">Dashboard</h1><p className="mt-1 text-slate-500">Overview for {String(membership.role).replaceAll("_"," ")}.</p><section className="mt-7 grid grid-cols-2 gap-3 lg:grid-cols-4">{[["Active students",students||0],["Staff",staff||0],["Classes",classes||0],["Absent today",pending||0]].map(([n,v])=><div key={n} className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{n}</p><b className="mt-1 block text-3xl">{v}</b></div>)}</section><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map(([name,href])=><Link key={name} href={href} className="rounded-2xl border bg-white p-6 shadow-sm hover:shadow-md"><h2 className="text-lg font-semibold">{name}</h2><p className="mt-2 text-sm text-slate-500">Open module →</p></Link>)}</section></div></main>}