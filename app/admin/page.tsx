import {redirect} from "next/navigation";import {requireSchoolUser,type SchoolRole} from "@/lib/auth";
export default async function Page(){const {membership}=await requireSchoolUser();const role=membership.role as SchoolRole;if(role==="teacher")redirect("/admin/teacher-dashboard");if(role==="accountant")redirect("/admin/accountant-dashboard");if(role==="reception")redirect("/admin/reception-dashboard");redirect("/admin/principal-dashboard")}