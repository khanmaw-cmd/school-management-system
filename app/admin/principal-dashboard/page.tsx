import Link from "next/link";import {requireSchoolUser} from "@/lib/auth";
export default async function Page(){const {supabase:s,school}=await requireSchoolUser(["school_owner","principal"]);const id=school!.id,today=new Date().toISOString().slice(0,10);const [{count:students},{count:staff},{count:absent},{data:payments},{data:charges},{data:exams}]=await Promise.all([s.from("students").select("*",{count:"exact",head:true}).eq("school_id",id).eq("active",true),s.from("staff").select("*",{count:"exact",head:true}).eq("school_id",id).eq("active",true),s.from("student_attendance").select("*",{count:"exact",head:true}).eq("school_id",id).eq("attendance_date",today).eq("status","absent"),s.from("fee_payments").select("amount").eq("school_id",id).eq("payment_date",today),s.from("student_fee_charges").select("amount,discount_amount,fee_payment_allocations(amount)").eq("school_id",id).eq("status","open"),s.from("exams").select("id,name,published").eq("school_id",id).order("created_at",{ascending:false}).limit(5)]);const collected=(payments||[]).reduce((n,x)=>n+Number(x.amount),0),dues=(charges||[]).reduce((n:any,x:any)=>n+Math.max(0,Number(x.amount)-Number(x.discount_amount)-(x.fee_payment_allocations||[]).reduce((a:number,p:any)=>a+Number(p.amount),0)),0);return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Principal Dashboard</h1><p className="text-slate-500">{school?.name} · operational overview</p><div className="mt-6 grid grid-cols-2 gap-3 lg:grid-cols-5">{[["Students",students||0],["Staff",staff||0],["Absent today",absent||0],["Collected today","₹"+collected.toFixed(2)],["Outstanding","₹"+dues.toFixed(2)]].map(([a,b])=><div key={a} className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{a}</p><b className="mt-1 block text-2xl">{b}</b></div>)}</div><div className="mt-6 grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Academic operations</h2><div className="mt-3 grid grid-cols-2 gap-2">{[["Attendance","/admin/attendance"],["Timetable","/admin/timetable"],["Teacher allocation","/admin/teacher-assignments"],["Results","/admin/exams/results"]].map(([n,h])=><Link key={h} href={h} className="rounded-xl border p-3 text-sm hover:bg-slate-50">{n}</Link>)}</div></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Recent exams</h2>{exams?.map(x=><div key={x.id} className="flex justify-between border-b py-3 text-sm"><span>{x.name}</span><span>{x.published?"Published":"Draft"}</span></div>)}</section></div></div></main>}