"use server";import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";
export async function switchSchool(f:FormData){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const schoolId=String(f.get("school_id"));const {data:m}=await s.from("school_memberships").select("school_id").eq("user_id",user.id).eq("school_id",schoolId).eq("active",true).maybeSingle();if(!m)throw new Error("School access not available");const {error}=await s.from("user_school_preferences").upsert({user_id:user.id,school_id:schoolId,updated_at:new Date().toISOString()},{onConflict:"user_id"});if(error)throw new Error(error.message);redirect("/admin");}