import Link from "next/link"; import { createClient } from "@/lib/supabase/server";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:m}=await s.from("school_memberships").select("school_id").eq("user_id",user!.id).eq("active",true).limit(1).single();const {data:students}=await s.from("students").select("id,admission_no,first_name,last_name,active").eq("school_id",m!.school_id).order("created_at",{ascending:false});return <main className="p-6 md:p-10"><div className="mx-auto max-w-6xl"><div className="flex justify-between"><div><h1 className="text-3xl font-bold">Students</h1><p className="text-slate-500">Admissions and enrollment.</p></div><Link href="/admin/students/new" className="rounded-xl bg-slate-900 px-5 py-3 text-white">+ New admission</Link></div><div className="mt-6 rounded-2xl border bg-white">{students?.length?students.map(x=><div key={x.id} className="grid grid-cols-3 border-b p-4 text-sm"><span>{x.admission_no}</span><span>{x.first_name} {x.last_name}</span><span>{x.active?"Active":"Inactive"}</span></div>):<p className="p-8 text-center text-slate-500">No students yet.</p>}</div></div></main>}