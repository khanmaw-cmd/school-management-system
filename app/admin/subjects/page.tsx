import {createClient} from "@/lib/supabase/server";import {addSubject} from "./actions";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:m}=await s.from("school_memberships").select("school_id").eq("user_id",user!.id).eq("active",true).limit(1).single();const {data:subjects}=await s.from("subjects").select("*").eq("school_id",m!.school_id).order("name");return <main className="p-6 md:p-10"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Subjects</h1><form action={addSubject} className="mt-6 flex gap-3 rounded-2xl border bg-white p-5"><input name="name" required placeholder="Subject name" className="flex-1 rounded-xl border p-3"/><input name="code" placeholder="Code" className="w-32 rounded-xl border p-3"/><button className="rounded-xl bg-slate-900 px-5 text-white">Add</button></form><div className="mt-6 grid gap-3 md:grid-cols-3">{subjects?.map(x=><div key={x.id} className="rounded-xl border bg-white p-4"><b>{x.name}</b><p className="text-sm text-slate-500">{x.code||"No code"}</p></div>)}</div></div></main>}