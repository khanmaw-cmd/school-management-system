import {createClient} from "@/lib/supabase/server";import {addTeacher} from "./actions";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:m}=await s.from("school_memberships").select("school_id").eq("user_id",user!.id).eq("active",true).limit(1).single();const {data:staff}=await s.from("staff").select("*").eq("school_id",m!.school_id).order("full_name");return <main className="p-6 md:p-10"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Teachers & Staff</h1><form action={addTeacher} className="mt-6 rounded-2xl border bg-white p-5"><h2 className="font-semibold">Add teacher</h2><div className="grid gap-3 md:grid-cols-2">{["employee_no","full_name","email","phone"].map(n=><input key={n} name={n} required={n==="employee_no"||n==="full_name"} placeholder={n.replaceAll("_"," ")} className="mt-3 rounded-xl border p-3"/>)}</div><button className="mt-3 rounded-xl bg-slate-900 px-5 py-3 text-white">Add teacher</button></form><div className="mt-6 rounded-2xl border bg-white">{staff?.map(x=><div key={x.id} className="grid grid-cols-3 border-b p-4 text-sm"><span>{x.employee_no}</span><b>{x.full_name}</b><span>{x.phone||x.email||"-"}</span></div>)}</div></div></main>}