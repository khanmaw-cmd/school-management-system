import {requireSchoolUser} from "@/lib/auth";import {checkIn,checkOut} from "./actions";export default async function Page(){const {supabase:s,school}=await requireSchoolUser(["school_owner","principal","reception"]);const {data:rows}=await s.from("visitor_entries").select("*").eq("school_id",school!.id).order("check_in",{ascending:false}).limit(200);return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Front Office & Visitors</h1><form action={checkIn} className="mt-6 grid gap-2 rounded-2xl border bg-white p-5 md:grid-cols-5">{[["name","Visitor name"],["phone","Phone"],["purpose","Purpose"],["meet","Person to meet"],["idref","ID reference"]].map(x=><input key={x[0]} name={x[0]} required={x[0]==="name"||x[0]==="purpose"} placeholder={x[1]} className="rounded-xl border p-3"/>)}<button className="rounded-xl bg-slate-900 p-3 text-white md:col-span-5">Check in & create gate pass</button></form><section className="mt-6 rounded-2xl border bg-white">{rows?.map(x=><div key={x.id} className="grid gap-2 border-b p-4 text-sm md:grid-cols-6 md:items-center"><b>{x.visitor_name}</b><span>{x.gate_pass_no}</span><span>{x.purpose}</span><span>{x.person_to_meet||"-"}</span><span>{new Date(x.check_in).toLocaleString()}</span>{!x.check_out?<form action={checkOut}><input type="hidden" name="id" value={x.id}/><button className="rounded-lg border px-3 py-2">Check out</button></form>:<span>Out {new Date(x.check_out).toLocaleTimeString()}</span>}</div>)}</section></div></main>}