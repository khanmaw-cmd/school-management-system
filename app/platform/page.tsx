import Link from "next/link";import {requirePlatformAdmin} from "@/lib/platform-auth";
export default async function Page(){const {supabase:s}=await requirePlatformAdmin();const [{count:schools},{count:active},{count:trial},{count:suspended},{count:students}]=await Promise.all([s.from("schools").select("*",{count:"exact",head:true}),s.from("school_subscriptions").select("*",{count:"exact",head:true}).eq("status","active"),s.from("school_subscriptions").select("*",{count:"exact",head:true}).eq("status","trial"),s.from("school_subscriptions").select("*",{count:"exact",head:true}).eq("status","suspended"),s.from("students").select("*",{count:"exact",head:true}).eq("active",true)]);return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Platform Overview</h1><p className="text-slate-500">Multi-school SaaS operations.</p><div className="mt-6 grid grid-cols-2 gap-3 lg:grid-cols-5">{[["Schools",schools||0],["Active subscriptions",active||0],["Trials",trial||0],["Suspended",suspended||0],["Active students",students||0]].map(([a,b])=><div key={a} className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{a}</p><b className="mt-1 block text-2xl">{b}</b></div>)}</div><div className="mt-6 grid gap-4 md:grid-cols-2"><Link href="/platform/schools" className="rounded-2xl border bg-white p-6"><b>Manage schools</b><p className="mt-1 text-sm text-slate-500">Review tenants and subscription state.</p></Link><Link href="/platform/plans" className="rounded-2xl border bg-white p-6"><b>Subscription plans</b><p className="mt-1 text-sm text-slate-500">Manage pricing, limits and plan availability.</p></Link></div></div></main>}