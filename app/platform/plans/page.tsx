import {requirePlatformAdmin} from "@/lib/platform-auth";
export default async function Page(){const {supabase:s}=await requirePlatformAdmin();const {data:p}=await s.from("subscription_plans").select("*").order("monthly_price");return <main className="p-6 md:p-10"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Subscription Plans</h1><p className="text-slate-500">Billing gateway is intentionally not connected yet; this is the SaaS entitlement foundation.</p><div className="mt-6 grid gap-5 md:grid-cols-3">{p?.map(x=><article key={x.id} className="rounded-2xl border bg-white p-6"><p className="text-sm font-semibold text-blue-700">{x.code.toUpperCase()}</p><h2 className="mt-1 text-xl font-bold">{x.name}</h2><p className="mt-3 text-3xl font-bold">₹{Number(x.monthly_price).toLocaleString("en-IN")}<span className="text-sm font-normal text-slate-500">/month</span></p><div className="mt-5 space-y-2 text-sm"><p>Students: {x.student_limit||"Custom / unlimited"}</p><p>Staff: {x.staff_limit||"Custom / unlimited"}</p><p>Status: {x.active?"Active":"Inactive"}</p></div></article>)}</div></div></main>}