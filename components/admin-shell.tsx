"use client";
import Link from "next/link";import {useEffect,useMemo,useState} from "react";import LogoutButton from "@/components/logout-button";
type Item={name:string;href:string};
export default function AdminShell({school,role,email,nav,multi,children}:{school:string;role:string;email:string;nav:Item[];multi:boolean;children:React.ReactNode}){const [open,setOpen]=useState(false),[cmd,setCmd]=useState(false),[q,setQ]=useState("");useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setCmd(v=>!v)}if(e.key==="Escape"){setCmd(false);setOpen(false)}};addEventListener("keydown",fn);return()=>removeEventListener("keydown",fn)},[]);const found=useMemo(()=>nav.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())).slice(0,10),[nav,q]);return <div className="min-h-screen bg-slate-50 lg:flex"><aside className={(open?"block ":"hidden ")+"border-b bg-slate-950 text-white lg:block lg:min-h-screen lg:w-72 lg:shrink-0"}><div className="flex items-start justify-between p-5"><div><p className="text-xs uppercase tracking-[.18em] text-slate-400">School Management</p><h2 className="mt-1 text-lg font-bold">{school}</h2><p className="mt-1 text-xs capitalize text-slate-400">{role.replaceAll("_"," ")}</p></div><button aria-label="Close menu" onClick={()=>setOpen(false)} className="rounded-lg p-2 text-slate-300 lg:hidden">✕</button></div><nav aria-label="Administration" className="max-h-[70vh] space-y-1 overflow-y-auto px-3 pb-5 lg:max-h-[calc(100vh-110px)]">{nav.map(x=><Link key={x.href} href={x.href} onClick={()=>setOpen(false)} className="block rounded-xl px-3 py-2.5 text-sm text-slate-200 transition hover:bg-slate-800 hover:text-white">{x.name}</Link>)}</nav></aside><div className="min-w-0 flex-1"><header className="sticky top-0 z-30 flex items-center justify-between border-b bg-white/90 px-4 py-3 backdrop-blur md:px-6"><div className="flex min-w-0 items-center gap-3"><button aria-label="Open menu" onClick={()=>setOpen(true)} className="rounded-xl border px-3 py-2 lg:hidden">☰</button><div className="min-w-0"><p className="truncate text-sm font-semibold">{school}</p><p className="truncate text-xs text-slate-500">{email}</p></div></div><div className="flex items-center gap-2"><button onClick={()=>setCmd(true)} className="hidden rounded-xl border px-3 py-2 text-sm text-slate-600 sm:block">Search <kbd className="ml-2 text-xs">⌘K</kbd></button>{multi&&<Link href="/admin/school-switcher" className="hidden rounded-xl border px-3 py-2 text-sm md:block">Switch school</Link>}<Link href="/notifications" aria-label="Notifications" className="rounded-xl border px-3 py-2 text-sm">Notifications</Link><LogoutButton/></div></header>{children}</div>{cmd&&<div className="fixed inset-0 z-50 bg-slate-950/40 p-4 pt-[12vh] backdrop-blur-sm" onMouseDown={()=>setCmd(false)}><div role="dialog" aria-modal="true" aria-label="Quick navigation" onMouseDown={e=>e.stopPropagation()} className="mx-auto max-w-xl overflow-hidden rounded-2xl border bg-white shadow-2xl"><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search pages…" className="w-full border-b p-4 text-base outline-none"/><div className="max-h-80 overflow-y-auto p-2">{found.map(x=><Link key={x.href} href={x.href} onClick={()=>setCmd(false)} className="block rounded-xl px-4 py-3 text-sm hover:bg-slate-100">{x.name}</Link>)}{!found.length&&<p className="p-4 text-sm text-slate-500">No matching page</p>}</div><p className="border-t px-4 py-2 text-xs text-slate-400">Esc to close · Ctrl/⌘ K to open</p></div></div>}</div>}